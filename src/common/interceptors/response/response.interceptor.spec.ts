import { Common\interceptors\responseInterceptor } from './common\interceptors\response.interceptor';

describe('Common\interceptors\responseInterceptor', () => {
  it('should be defined', () => {
    expect(new Common\interceptors\responseInterceptor()).toBeDefined();
  });
});
